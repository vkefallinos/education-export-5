---
description: "Generates scoring rubric section"
type: "updateFlowOutput"
model: "claude-3-5-sonnet"
temperature: "0.3"
---
Create a clear, criterion-referenced scoring rubric with performance indicators for each level (Exemplary, Proficient, Developing, Beginning). Each criterion should have specific descriptors that distinguish between performance levels. Include point values for each criterion. The rubric should be user-friendly for both teachers and students.

<output target="rubric">
{
  "type": "object",
  "properties": {
    "sectionTitle": {
      "type": "string",
      "description": "Section title (e.g., Scoring Rubric)"
    },
    "sectionInstructions": {
      "type": "string",
      "description": "How to use the rubric"
    },
    "criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "criterion": {
            "type": "string"
          },
          "exemplary": {
            "type": "string"
          },
          "proficient": {
            "type": "string"
          },
          "developing": {
            "type": "string"
          },
          "beginning": {
            "type": "string"
          },
          "points": {
            "type": "number"
          }
        }
      }
    }
  }
}
</output>